# TradingSandbox Build Configuration
# Customize ports, URLs, and build settings here

# Service Ports (for dev servers)
API_PORT=8080
FRONTEND_PORT=5173
DATA_PORT=8000

# Database Configuration (used by docker-compose and application)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tradingsandbox

# Application Environment Variables
# These are exported when running services via bb/make
ENV_FILE="TradingSandboxAPI/.env"

# Build Settings
MAVEN_OPTS="-DskipTests"
NPM_INSTALL_OPTS=""
PYTHON_VENV_DIR="venv"

# Run Settings
SPRING_PROFILE="dev"
VITE_HOST="localhost"
UVICORN_RELOAD="--reload"

# Service Directories (relative to project root)
API_DIR="TradingSandboxAPI"
FRONTEND_DIR="TradingSandbox-FrontEnd"
DATA_DIR="DataAcquisition"

# Color Output (set to 0 to disable colors)
USE_COLORS=1
